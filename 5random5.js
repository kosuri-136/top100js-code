// generating  a random number
const b = Math.floor(Math.random()*10);
console.log(b);

// input from the user
const min = 1
const max = 100

// generating a random number
const a = Math.floor(Math.random() * 100 (max - min + 1)) + min;

// display a random number
console.log(`Random value between ${min} and ${max} is ${a}`);